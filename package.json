{
  "name": "wheel",
  "version": "3.8.40",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --target=chrome74 --outfile=dist/app.js --sourcemap --external:path --external:url --external:fs --external:fs/promises --external:ajv",
    "test": "node --test",
    "lint:forbidden": "node --test test/forbidden-literals.test.js"
  },
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
