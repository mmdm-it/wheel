{
  "name": "wheel",
  "version": "3.11.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/split-catalog.mjs && esbuild src/main.js --bundle --format=iife --target=chrome74 --outfile=dist/app.js --sourcemap --external:path --external:url --external:fs --external:fs/promises --external:ajv && node scripts/precompress-json.mjs",
    "test": "node --test && npm run lint:forbidden",
    "lint:forbidden": "node --test test/forbidden-literals.test.js"
  },
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "GPL-3.0-only",
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
